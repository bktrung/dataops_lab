[sqlfluff]
dialect = tsql
max_line_length = 120

# The project uses AdventureWorks/SQL Server mixed-case identifiers.
# Enforce consistency via formatting, but don't require lower-case identifiers.
exclude_rules = CP02
